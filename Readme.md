# Todo
* Change sound output device
* Notifications manager
* How does minimized apps works? 
* Tree view in nvim
# Dependencies
From aur
    `qtile-extras-git`
    `newtorkmanager-dmenu` and run `ln -s /usr/bin/rofi /usr/bin/dmenu)`
    `rofi-power-menu`
    `rofi-bluetooth-git`


